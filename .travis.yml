python:
  - 2.7
  - 3.5
  - 3.6
language: python
install:
  - pip install setuptools
  - pip install olefile
  - pip install lxml
  - pip install -r requirements.txt
script: py.test
after_failure: cat test/diffengine.log
